<view class="count-down" wx:if="{{uiShow}}">
    <span>{{day}}</span>
    <span>:</span>
    <span>{{hour}}</span>
    <span>:</span>
    <span>{{minutes}}</span>
    <span>:</span>
    <span>{{second}}</span>
    <span>:</span>
    <span>{{millSecond}}</span>
</view>
